/*
1. Math.abs(x) => Gives absolute value of a number by removing minus sign or negative sign.
*/
let number = -3;
// console.log(Math.abs(number));
/*
2. Math.ceil(x) => Round the number up to nearest integer . 
*/
let n = 3.3;
// console.log(Math.ceil(n));
/*
3. Math.floor(x) => Round the number down to nearest integer. 
*/
// console.log(Math.floor(n));
/*
4. Math.round(x) => Round the number (0.5 numbers are round up) . 
*/
// console.log(Math.round(n));

/*
5. Math.trunc(x) => Removes the decimal part of number. 
*/
let x = 3.3;
// console.log(Math.trunc(x));
/*
6. Math.pow(base,exponent) => Gives power of the number (base^exponent) 
*/
// console.log(Math.pow(2, 3));
/*
7. Math.sqrt(x) => Gives square root of the number.
*/
// console.log(Math.sqrt(n)); 

/*
8. Math.cbrt(x) => Gives the cube root of the number.
*/
let y = 27;
// console.log(Math.cbrt(y));

/*
9. Math.max(a, b, c, ...) => Returns the largest number from the given values.
*/
let a = 10, b = 20, c = 5;
// console.log(Math.max(a, b, c));

/*
10. Math.min(a, b, c, ...) => Returns the smallest number from the given values.
*/
// console.log(Math.min(a, b, c));

/*
11. Math.random() => Generates a random number between 0 and 1 (not including 1).
*/
// console.log(Math.random());

/*
12. Math.sin(x) => Returns the sine of an angle in radians.
*/
let angle = Math.PI / 2;
// console.log(Math.sin(angle));

/*
13. Math.cos(x) => Returns the cosine of an angle in radians.
*/
// console.log(Math.cos(angle));

/*
14. Math.tan(x) => Returns the tangent of an angle in radians.
*/
// console.log(Math.tan(angle));

/*
15. Math.log(x) => Returns the natural logarithm (ln) of a number.
*/
let num = Math.E;
// console.log(Math.log(num));

/*
16. Math.log10(x) => Returns the base-10 logarithm of a number.
*/
let value = 100;
// console.log(Math.log10(value));

/*
17. Math.exp(x) => Returns e^x (exponential function).
*/
let exponent = 2;
// console.log(Math.exp(exponent));

/*
18. Math.sign(x) => Returns 1 for positive, -1 for negative, and 0 for zero.
*/
let check = -5;
// console.log(Math.sign(check));

/*
19. Math.hypot(a, b, ...) => Returns the hypotenuse (square root of the sum of squares).
*/
let side1 = 3, side2 = 4;
// console.log(Math.hypot(side1, side2));
